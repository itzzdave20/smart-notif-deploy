# Railway configuration
[build]
builder = "nixpacks"

[deploy]
startCommand = "streamlit run smart-notification-app.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
